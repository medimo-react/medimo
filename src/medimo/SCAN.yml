info:
  name: SCAN
  type: http
  seq: 4

http:
  method: GET
  url: http://127.0.0.1:5000/api/medicine?q=코데농정&refresh=true
  params:
    - name: q
      value: 코데농정
      type: query
    - name: refresh
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
